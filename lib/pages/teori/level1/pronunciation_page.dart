import 'package:flutter/material.dart';

class PronunciationPage extends StatelessWidget {
  const PronunciationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("pronoun"),),
    );
  }
}
